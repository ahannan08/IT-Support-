# IT Support

React frontend. Content for each page lives in JSON and is added as you provide it.

```bash
cd frontend
npm install
npm run dev
```

- Components: `frontend/src/components`
- Pages: `frontend/src/pages`
- Content: `frontend/src/content/*.json`
- App: `frontend/src/App.jsx`
